# Call RcppXsimd to get the correct SSE flags for the current hardware
PKG_CFLAGS = $(shell "${R_HOME}/bin/Rscript" -e "cat(RcppXsimd::getSSEFlags(), RcppXsimd::getNEONFlags())")
PKG_CXXFLAGS = $(shell "${R_HOME}/bin/Rscript" -e "cat(RcppXsimd::getSSEFlags(), RcppXsimd::getNEONFlags())")
